<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base App Theme -->
    <style name="Theme.MyVideoteca" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colori principali -->
        <item name="colorPrimary">@color/mv_purple</item>
        <item name="colorOnPrimary">@color/mv_red</item>
        <item name="colorSecondary">@color/mv_yellow</item>
        <item name="android:windowBackground">@color/mv_purple</item>

        <!-- Imposta il colore della barra in alto -->
        <item name="colorPrimaryVariant">@color/mv_purple</item>
        <item name="android:statusBarColor">@color/mv_purple</item>

        <!-- Stile del pulsante -->
        <item name="materialButtonStyle">@style/PurpleButton</item>
    </style>
</resources>